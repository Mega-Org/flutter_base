# App architecture

## Purpose
Documents this Flutter app’s core vs feature boundaries and where shared infrastructure lives so agents align with the real `lib/` layout.

## Fill when
- After you lock how `lib/core/` is structured (barrel, foundation, network, DI, blocs, configs, localization, etc.).
- When you split or merge modules, flavors, or packages in a monorepo.

## References
- `lib/core/` in this repository (primary ground truth).
- `ai_toolkit/rules/core/_index.md` for stack-wide expectations that must not contradict this file.

## Content
<!-- Fill in later: summarize folders under lib/, what belongs in core vs features, and pointers to key entrypoints. -->
