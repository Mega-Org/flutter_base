# App conventions

## Purpose
Captures naming, folder layout, and app-level exceptions so AI-assisted edits stay consistent with this codebase.

## Fill when
- After you standardize file naming, feature folder shapes, or import style beyond the shared toolkit.
- When this app intentionally differs from `ai_toolkit` defaults (document the exception here).

## References
- `ai_toolkit/patterns/dart/absolute-imports.md` and `ai_toolkit/rules/dart/imports-and-analysis.md` for import and analyzer policy.
- `ai_docs/architecture.md` for where code belongs.

## Content
<!-- Fill in later: naming tables, feature template, and any deliberate deviations from ai_toolkit defaults. -->
